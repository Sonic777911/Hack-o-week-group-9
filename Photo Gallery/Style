:root{
  --bg0:#0b1020;
  --bg1:#0f1730;
  --text:#e9eefc;
  --muted:#aab4d6;
  --card:#121b38;
  --shadow: 0 10px 30px rgba(0,0,0,.35);
  --shadow-strong: 0 18px 50px rgba(0,0,0,.55);
  --ring: rgba(125, 211, 252, .55);
  --accent:#7dd3fc;
}

*{ box-sizing:border-box; }

html, body { height: 100%; }

body{
  margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
  color:var(--text);
  background:
    radial-gradient(1200px 800px at 15% 10%, rgba(125, 211, 252, .25), transparent 55%),
    radial-gradient(900px 700px at 80% 0%, rgba(167, 139, 250, .25), transparent 50%),
    linear-gradient(160deg, var(--bg0), var(--bg1));
}

.page-header{
  max-width: 1100px;
  margin: 28px auto 0;
  padding: 0 18px;
}

.page-title{
  margin: 0;
  font-weight: 800;
  letter-spacing: .2px;
  font-size: clamp(1.4rem, 2.5vw, 2.1rem);
}

.page-subtitle{
  margin: 10px 0 0;
  color: var(--muted);
  line-height: 1.45;
}

.gallery-area{
  max-width: 1100px;
  margin: 18px auto 40px;
  padding: 0 18px;
}

.gallery-note{
  max-width: 1100px;
  margin: -28px auto 40px;
  padding: 0 18px;
  color: var(--muted);
}

.gallery-note p{
  margin: 0;
  line-height: 1.6;
  font-size: 1rem;
}

.gallery{
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
}

/* Card button */
.card{
  appearance: none;
  border: 1px solid rgba(255,255,255,.06);
  background: rgba(18, 27, 56, .35);
  border-radius: 14px;
  padding: 0;
  overflow: hidden;
  position: relative;
  cursor: pointer;
  box-shadow: 0 0 0 rgba(0,0,0,0);
  outline: none;

  /* Keep hover effects visually smooth and without layout reflow. */
  transform: translateZ(0) scale(1);
  transition: transform 240ms ease, box-shadow 240ms ease, border-color 240ms ease;
  will-change: transform;
}

.card, .card *{
  transform-style: preserve-3d;
}

.card:focus-visible{
  box-shadow: 0 0 0 3px var(--ring);
}

.card-caption{
  padding: 10px 12px 12px;
  font-size: .95rem;
  line-height: 1.3;
  color: rgba(233, 238, 252, .9);
  background: rgba(18, 27, 56, .55);
  border-top: 1px solid rgba(255,255,255,.06);
  transition: color 220ms ease;
}

.card:hover .card-caption{
  color: rgba(255,255,255,.98);
}

/* Stable aspect ratio container to avoid layout distortion */
.card-media{
  width: 100%;
  aspect-ratio: 4 / 3;
  overflow: hidden;
}

.card-media img{
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;

  transform: scale(1);
  transition: transform 260ms ease, filter 260ms ease;
  will-change: transform;
}

/* Hover effect: zoom + subtle highlight, without moving other tiles */
.card:hover{
  z-index: 10;
  border-color: rgba(125, 211, 252, .35);
  box-shadow: var(--shadow);
  transform: translateZ(0) scale(1.06);
}

.card:hover .card-media img{
  transform: scale(1.14);
  filter: saturate(1.05) contrast(1.02);
}

.card::after{
  content:"";
  position:absolute;
  inset:0;
  background:
    radial-gradient(700px 260px at 50% 0%, rgba(125, 211, 252, .18), transparent 60%);
  opacity: 0;
  transition: opacity 220ms ease;
  pointer-events:none;
}

.card:hover::after{
  opacity: 1;
}

/* Lightbox */
.lightbox{
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.94);
  display: none;
  align-items: center;
  justify-content: center;
  padding: 24px 18px;
}

.lightbox.is-open{
  display: flex;
}

.lightbox-media{
  width: min(1100px, 92vw);
  display: grid;
  grid-template-rows: auto auto;
  gap: 10px;
}

.lightbox-media img{
  width: 100%;
  max-height: 78vh;
  object-fit: contain;
  border-radius: 14px;
  box-shadow: var(--shadow-strong);

  transform: scale(.98);
  opacity: .0;
  transition: transform 220ms ease, opacity 220ms ease;
}

.lightbox.is-open .lightbox-media img{
  transform: scale(1);
  opacity: 1;
}

.lightbox-caption{
  color: rgba(255,255,255,.85);
  font-size: .95rem;
  line-height: 1.4;
  text-align: center;
}
.lightbox-nav {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 50px;
  height: 50px;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.2);
  background: rgba(255,255,255,0.08);
  color: white;
  font-size: 28px;
  cursor: pointer;
  display: grid;
  place-items: center;
  z-index: 20;
}

.lightbox-nav:hover {
  background: rgba(125, 211, 252, 0.2);
  border-color: rgba(125, 211, 252, 0.5);
}

.prev {
  left: 20px;
}

.next {
  right: 20px;
}

.lightbox-close{
  position: absolute;
  top: 16px;
  right: 16px;
  width: 42px;
  height: 42px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(255,255,255,.06);
  color: rgba(255,255,255,.92);
  font-size: 28px;
  line-height: 1;
  cursor: pointer;
  display: grid;
  place-items: center;
}

.lightbox-close:hover{
  border-color: rgba(125, 211, 252, .45);
  box-shadow: 0 0 0 3px rgba(125, 211, 252, .25);
}

.lightbox-close:focus-visible{
  outline: none;
  box-shadow: 0 0 0 3px var(--ring);
}

body.is-lightbox-open{
  overflow: hidden;
}

@media (max-width: 520px){
  .gallery{ gap: 12px; }
  .card-media{ aspect-ratio: 1 / 1; }
  .lightbox-close{ top: 12px; right: 12px; }
}

@media (hover: none){
  .card:hover{
    transform: translateZ(0) scale(1);
  }
  .card:hover .card-media img{
    transform: scale(1);
    filter: none;
  }
}

@media (prefers-reduced-motion: reduce){
  .card{
    transition: none;
  }
  .card-media img{
    transition: none;
  }
  .card:hover{
    transform: translateZ(0) scale(1);
  }
  .card:hover .card-media img{
    transform: scale(1);
    filter: none;
  }
}
