:root {
  --bg-gradient: linear-gradient(135deg, #6a11cb 0%, #2575fc 50%, #2ec4b6 100%);
  --card-bg: rgba(255, 255, 255, 0.92);
  --text-main: #1e2a44;
  --text-muted: #52607a;
  --accent: #ff7b54;
  --shadow: 0 10px 30px rgba(20, 30, 80, 0.18);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  min-height: 100vh;
  font-family: "Poppins", sans-serif;
  background: var(--bg-gradient);
  color: var(--text-main);
  padding: 28px 14px 36px;
}

.page-container {
  width: min(1120px, 100%);
  margin: 0 auto;
}

.hero {
  text-align: center;
  color: #ffffff;
  margin-bottom: 24px;
}

.hero h1 {
  font-size: clamp(1.8rem, 4vw, 2.9rem);
  letter-spacing: 0.4px;
}

.hero p {
  margin-top: 8px;
  font-weight: 300;
  opacity: 0.95;
}

.filters {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 10px;
  margin: 0 0 20px;
}

.filter-btn {
  background: rgba(255, 255, 255, 0.2);
  color: #ffffff;
  border: 1px solid rgba(255, 255, 255, 0.45);
  padding: 8px 14px;
  border-radius: 999px;
  font-family: inherit;
  font-size: 0.9rem;
  cursor: pointer;
  transition: transform 0.25s ease, background-color 0.25s ease, box-shadow 0.25s ease;
}

.filter-btn:hover,
.filter-btn:focus-visible {
  transform: translateY(-2px);
  background: rgba(255, 255, 255, 0.3);
  box-shadow: 0 6px 16px rgba(10, 20, 60, 0.2);
}

.filter-btn.active {
  background: #ffffff;
  color: #1c2a4f;
  border-color: #ffffff;
  font-weight: 600;
}

.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(235px, 1fr));
  gap: 18px;
}

.city-card {
  background: var(--card-bg);
  border-radius: 16px;
  overflow: hidden;
  box-shadow: var(--shadow);
  transition: transform 0.35s ease, box-shadow 0.35s ease, border 0.35s ease;
  border: 2px solid transparent;
  cursor: pointer;
}

.city-card.hidden {
  display: none;
}

.city-card:hover,
.city-card:focus-visible,
.city-card.active {
  transform: translateY(-7px) scale(1.02);
  box-shadow: 0 16px 34px rgba(20, 30, 80, 0.28);
  border-color: rgba(37, 117, 252, 0.55);
}

.landmark-img {
  width: 100%;
  height: 160px;
  object-fit: cover;
  display: block;
}

.card-content {
  padding: 14px 14px 16px;
}

.card-content h2 {
  font-size: 1.3rem;
}

.landmark {
  color: var(--text-muted);
  font-size: 0.9rem;
  margin: 2px 0 10px;
}

.weather-icon {
  font-size: 1.7rem;
  margin-bottom: 8px;
}

.weather-list {
  list-style: none;
  display: grid;
  gap: 6px;
  font-size: 0.9rem;
}

.weather-list li {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  border-bottom: 1px dashed rgba(82, 96, 122, 0.25);
  padding-bottom: 5px;
}

.weather-list li span {
  color: var(--text-muted);
}

.weather-list li strong {
  color: var(--text-main);
  font-weight: 600;
  text-align: right;
}

@media (max-width: 600px) {
  body {
    padding: 22px 10px 26px;
  }

  .card-content {
    padding: 12px;
  }
}
