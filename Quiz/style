/* =========================================
   Global page styling + blue theme background
   ========================================= */
body {
    margin: 0;
    padding: 0;
    font-family: Arial, Helvetica, sans-serif;

    /* Blue theme + background photo + soft dark overlay for readability */
    background-image:
        linear-gradient(rgba(15, 23, 42, 0.75), rgba(15, 23, 42, 0.75)),
        /* Replace this file name with your own image if you like */
        url("C:\Users\asus\Downloads\WhatsApp Image 2026-02-25 at 7.06.04 PM.jpeg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;

    color: #e5e7eb;
}

/* Center the quiz card on the page */
.page-wrapper {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 16px;
}

/* =========================================
   Quiz card container
   ========================================= */
.quiz-card {
    background: linear-gradient(135deg, #f9fbff, #eef4ff);
    max-width: 650px;
    width: 100%;
    border-radius: 18px;
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.35);
    padding: 22px 26px 26px 26px;
    box-sizing: border-box;
    color: #0f172a;
    border: 1px solid rgba(191, 219, 254, 0.9);
    backdrop-filter: blur(6px);
}

/* Header with logo and college name */
.header {
    display: flex;
    align-items: center;
    border-bottom: 1px solid #dbeafe;
    padding-bottom: 12px;
    margin-bottom: 16px;
}

.college-logo {
    width: 72px;
    height: 72px;
    border-radius: 16px;
    margin-right: 16px;
    object-fit: cover;
    border: 2px solid #60a5fa;
    background-color: #eff6ff;
    box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.15);
}

/* College text styling */
.college-info h1 {
    font-size: 1.25rem;
    margin: 0;
    color: #1d4ed8;
    letter-spacing: 0.03em;
}

.college-info h2 {
    font-size: 1.05rem;
    margin: 3px 0 5px 0;
    color: #2563eb;
}

.subtitle {
    margin: 0;
    font-size: 0.9rem;
    color: #64748b;
    font-weight: 600;
}

/* =========================================
   Progress and question styling
   ========================================= */
.progress-text {
    font-size: 0.95rem;
    color: #6b7280;
    margin: 6px 0 12px 0;
    font-weight: 600;
}

/* Question box styling */
.question-box {
    background: radial-gradient(circle at top left, #e0f2fe, #eff6ff);
    border-radius: 12px;
    padding: 14px 16px;
    margin-bottom: 18px;
    border: 1px solid #bfdbfe;
    box-shadow: inset 0 0 0 1px rgba(191, 219, 254, 0.4);
}

.question-box h3 {
    margin: 0;
    font-size: 1.05rem;
    color: #0f172a;
}

/* =========================================
   Options styling
   ========================================= */
.options-form {
    margin-bottom: 10px;
}

/* Each option row */
.option {
    display: flex;
    align-items: center;
    margin-bottom: 9px;
    padding: 8px 10px;
    border-radius: 999px;
    transition: background-color 0.15s ease, border-color 0.15s ease, transform 0.05s ease;
    border: 1px solid transparent;
    background-color: #f9fafb;
    cursor: pointer;
}

.option:hover {
    background-color: #e0f2fe;
    border-color: #60a5fa;
    transform: translateY(-1px);
}

/* Radio button */
.option input[type="radio"] {
    margin-right: 8px;
    accent-color: #2563eb; /* modern browsers */
}

/* Label for option */
.option label {
    cursor: pointer;
    font-size: 0.96rem;
}

/* =========================================
   Feedback styling
   ========================================= */
.feedback {
    min-height: 24px;
    font-weight: 700;
    margin-bottom: 10px;
    font-size: 0.97rem;
}

/* Feedback colors */
.feedback.correct {
    color: #047857; /* green */
}

.feedback.incorrect {
    color: #b91c1c; /* red */
}

/* =========================================
   Controls: score + buttons
   ========================================= */
.controls {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 8px;
    margin-bottom: 8px;
}

.score-display {
    font-size: 0.95rem;
    color: #374151;
    font-weight: 600;
}

/* Primary buttons */
.next-btn,
.restart-btn {
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    color: #ffffff;
    border: none;
    padding: 9px 18px;
    border-radius: 999px;
    font-size: 0.95rem;
    cursor: pointer;
    transition: background 0.15s ease, transform 0.05s ease, box-shadow 0.15s ease;
    box-shadow: 0 8px 18px rgba(37, 99, 235, 0.35);
}

.next-btn:disabled {
    background: #9ca3af;
    cursor: not-allowed;
    box-shadow: none;
}

.next-btn:hover:not(:disabled),
.restart-btn:hover {
    background: linear-gradient(135deg, #1d4ed8, #1e40af);
    box-shadow: 0 10px 22px rgba(30, 64, 175, 0.45);
}

.next-btn:active,
.restart-btn:active {
    transform: scale(0.97);
}

/* =========================================
   Result box
   ========================================= */
.result-box {
    margin-top: 18px;
    padding-top: 14px;
    border-top: 1px dashed #e5e7eb;
}

.result-box h3 {
    margin-top: 0;
    color: #1d4ed8;
}

/* Utility class to hide elements */
.hidden {
    display: none;
}

/* =========================================
   Responsive tweaks
   ========================================= */
@media (max-width: 480px) {
    .quiz-card {
        padding: 18px;
        border-radius: 14px;
    }

    .header {
        flex-direction: row;
        align-items: center;
    }

    .college-logo {
        width: 64px;
        height: 64px;
        margin-right: 10px;
    }

    .college-info h1 {
        font-size: 1.05rem;
    }

    .college-info h2 {
        font-size: 0.95rem;
    }

    .question-box h3 {
        font-size: 0.98rem;
    }

    .next-btn,
    .restart-btn {
        padding: 8px 12px;
    }
}
